# Lup's memory index (project-local)

This is Lup's working memory for `loop-lab`. Entries here apply only to this
project. Cross-project insight is occasionally distilled and shipped to the
operator's external memory store via the round meta-review; Lup does not
read from that store and does not need to.

## Files (one entry per file, semantically grouped)

(none yet — round 0 is bootstrap)

## Index meta

- last updated: 2026-05-17 (bootstrap, no entries)
- format: same frontmatter as the parent CC convention (name, description, type)
- types in use: `feedback`, `pattern`, `failure`, `reference`
